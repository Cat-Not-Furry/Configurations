# Generado por scripts/generate-tmux-colors.sh
# Tema: tokyo_night_moon (Tokyo Night Moon) — slug: tokyo-night-moon
# Copia manualmente a colors.active.conf o usa apply-theme.sh

# Plantilla de colores tmux — scripts/generate-tmux-colors.sh
# Placeholders: #222436 #c8d3f5 #82aaff #7dcfff #82aaff
# Genera tmux/colors/{slug}.conf desde themes/palettes.json (waybar)

set -g status-style 'bg=#222436,fg=#c8d3f5'
set -g status-left-style 'bg=#82aaff,fg=#222436,bold'
set -g status-right-style 'bg=#222436,fg=#c8d3f5'
set -g window-status-style 'bg=#222436,fg=#c8d3f5'
set -g window-status-current-style 'bg=#82aaff,fg=#222436,bold'
set -g pane-border-style 'fg=#82aaff'
set -g pane-active-border-style 'fg=#7dcfff'
set -g message-style 'bg=#82aaff,fg=#222436'
