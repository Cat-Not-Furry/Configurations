# Generado por scripts/generate-tmux-colors.sh
# Tema: classic (Clásico personal) — slug: classic
# Copia manualmente a colors.active.conf o usa apply-theme.sh

# Plantilla de colores tmux — scripts/generate-tmux-colors.sh
# Placeholders: #161821 #FFFFFF #84A0C6 #A2B9D6 #84A0C6
# Genera tmux/colors/{slug}.conf desde themes/palettes.json (waybar)

set -g status-style 'bg=#161821,fg=#FFFFFF'
set -g status-left-style 'bg=#84A0C6,fg=#161821,bold'
set -g status-right-style 'bg=#161821,fg=#FFFFFF'
set -g window-status-style 'bg=#161821,fg=#FFFFFF'
set -g window-status-current-style 'bg=#84A0C6,fg=#161821,bold'
set -g pane-border-style 'fg=#84A0C6'
set -g pane-active-border-style 'fg=#A2B9D6'
set -g message-style 'bg=#84A0C6,fg=#161821'
