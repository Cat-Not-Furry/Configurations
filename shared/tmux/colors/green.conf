# Generado por scripts/generate-tmux-colors.sh
# Tema: green (Verde olivo) — slug: green
# Copia manualmente a colors.active.conf o usa apply-theme.sh

# Plantilla de colores tmux — scripts/generate-tmux-colors.sh
# Placeholders: #141812 #EEF2E8 #8FA86A #A8C088 #8FA86A
# Genera tmux/colors/{slug}.conf desde themes/palettes.json (waybar)

set -g status-style 'bg=#141812,fg=#EEF2E8'
set -g status-left-style 'bg=#8FA86A,fg=#141812,bold'
set -g status-right-style 'bg=#141812,fg=#EEF2E8'
set -g window-status-style 'bg=#141812,fg=#EEF2E8'
set -g window-status-current-style 'bg=#8FA86A,fg=#141812,bold'
set -g pane-border-style 'fg=#8FA86A'
set -g pane-active-border-style 'fg=#A8C088'
set -g message-style 'bg=#8FA86A,fg=#141812'
