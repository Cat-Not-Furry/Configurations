# Generado por scripts/generate-tmux-colors.sh
# Tema: tokyo_night (Tokyo Night) — slug: tokyo-night
# Copia manualmente a colors.active.conf o usa apply-theme.sh

# Plantilla de colores tmux — scripts/generate-tmux-colors.sh
# Placeholders: #1a1b26 #a9b1d6 #7aa2f7 #73daca #7aa2f7
# Genera tmux/colors/{slug}.conf desde themes/palettes.json (waybar)

set -g status-style 'bg=#1a1b26,fg=#a9b1d6'
set -g status-left-style 'bg=#7aa2f7,fg=#1a1b26,bold'
set -g status-right-style 'bg=#1a1b26,fg=#a9b1d6'
set -g window-status-style 'bg=#1a1b26,fg=#a9b1d6'
set -g window-status-current-style 'bg=#7aa2f7,fg=#1a1b26,bold'
set -g pane-border-style 'fg=#7aa2f7'
set -g pane-active-border-style 'fg=#73daca'
set -g message-style 'bg=#7aa2f7,fg=#1a1b26'
