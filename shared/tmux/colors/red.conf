# Generado por scripts/generate-tmux-colors.sh
# Tema: red (Rojo carmesí) — slug: red
# Copia manualmente a colors.active.conf o usa apply-theme.sh

# Plantilla de colores tmux — scripts/generate-tmux-colors.sh
# Placeholders: #1A1214 #FCE8EA #C62828 #E53935 #C62828
# Genera tmux/colors/{slug}.conf desde themes/palettes.json (waybar)

set -g status-style 'bg=#1A1214,fg=#FCE8EA'
set -g status-left-style 'bg=#C62828,fg=#1A1214,bold'
set -g status-right-style 'bg=#1A1214,fg=#FCE8EA'
set -g window-status-style 'bg=#1A1214,fg=#FCE8EA'
set -g window-status-current-style 'bg=#C62828,fg=#1A1214,bold'
set -g pane-border-style 'fg=#C62828'
set -g pane-active-border-style 'fg=#E53935'
set -g message-style 'bg=#C62828,fg=#1A1214'
