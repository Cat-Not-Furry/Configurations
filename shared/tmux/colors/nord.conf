# Generado por scripts/generate-tmux-colors.sh
# Tema: nord (Nord) — slug: nord
# Copia manualmente a colors.active.conf o usa apply-theme.sh

# Plantilla de colores tmux — scripts/generate-tmux-colors.sh
# Placeholders: #2e3440 #eceff4 #88c0d0 #81a1c1 #88c0d0
# Genera tmux/colors/{slug}.conf desde themes/palettes.json (waybar)

set -g status-style 'bg=#2e3440,fg=#eceff4'
set -g status-left-style 'bg=#88c0d0,fg=#2e3440,bold'
set -g status-right-style 'bg=#2e3440,fg=#eceff4'
set -g window-status-style 'bg=#2e3440,fg=#eceff4'
set -g window-status-current-style 'bg=#88c0d0,fg=#2e3440,bold'
set -g pane-border-style 'fg=#88c0d0'
set -g pane-active-border-style 'fg=#81a1c1'
set -g message-style 'bg=#88c0d0,fg=#2e3440'
