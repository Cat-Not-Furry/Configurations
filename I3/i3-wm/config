# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

# Incluye los fragmentos modulares en orden.

set $mod Mod4

# Tema ventanas/gaps (antes de bar y exec; ver apply-i3-theme.sh).
include conf.d/07-gaps_borders.conf

# Fuente decoraciones (tabbed/stacking); tamaño 0 ≈ sin barra de título. La barra usa su propia fuente en 05-bbar.conf.
font pango: Hack Nerd Font 0

include conf.d/00-env.conf
include conf.d/00-exec.conf
include conf.d/01-binds_media.conf
include conf.d/02-binds_focus_move.conf
include conf.d/03-workspaces.conf
include conf.d/04-binds_misc.conf
include conf.d/05-bbar.conf
# include conf.d/06-sbar.conf
include conf.d/08-scripts.conf
include conf.d/09-copyq.conf
include conf.d/09-blueman.conf
